# 启用 AndroidX（必须）
android.useAndroidX=true
# 自动迁移第三方库到 AndroidX（必须）
android.enableJetifier=true

# JVM 内存设置（防止内存溢出，CI 环境推荐）
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
# 并行编译加速
org.gradle.parallel=true
