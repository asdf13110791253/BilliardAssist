# 素材补齐说明

项目代码已经完整，但以下 4 个文件需要你手动补齐（因为无法自动生成）。

---

## 1️⃣ OpenCV JAR 文件

**来源：** https://opencv.org/releases/ 下载 Android 版

**操作：**
1. 下载后解压，进入 `OpenCV-android-sdk/sdk/java/`
2. 找到 `opencv-4.x.x.jar` 文件
3. 复制到项目的 `app/libs/` 目录下
4. 重命名为 `opencv-android.jar`（或保持原名，Gradle 会自动识别）

---

## 2️⃣ OpenCV 原生库 (.so 文件)

**来源：** 同上 OpenCV Android SDK

**操作：**
1. 进入 `OpenCV-android-sdk/sdk/native/libs/`
2. 复制以下两个文件到对应目录：

| 源文件 | 目标路径 |
|---------|---------|
| `armeabi-v7a/libopencv_java4.so` | `app/src/main/jniLibs/armeabi-v7a/libopencv_java4.so` |
| `arm64-v8a/libopencv_java4.so` | `app/src/main/jniLibs/arm64-v8a/libopencv_java4.so` |

> 如果没有对应文件夹，请新建。

---

## 3️⃣ 瞄准环模板图片 (aim_template.png)

**这是最关键的文件！** OpenCV 靠它来在屏幕中定位瞄准环。

**获取方法：**
1. 打开你的台球游戏，进入有瞄准环的画面
2. 截图（音量下+电源键）
3. 用手机自带的图片编辑器或 MT 管理器裁剪：
   - 只保留**瞄准环本身**（圆形虚线/实线那个）
   - 去掉背景、球桌、球等一切多余内容
   - 建议尺寸：80×80 ~ 120×120 像素
4. 保存为 `aim_template.png`
5. 覆盖到 `app/src/main/res/drawable/aim_template.png`

**注意事项：**
- 模板必须和游戏中**完全一样**（颜色、大小、样式）
- 如果游戏更新后瞄准环变了，需要重新截图
- 模板匹配对分辨率敏感，建议在同一分辨率下使用

---

## 4️⃣ 验证清单

补齐后，检查以下文件是否存在：

```
app/libs/opencv-android.jar                          ✅ 或 opencv-4.x.x.jar
app/src/main/jniLibs/armeabi-v7a/libopencv_java4.so  ✅
app/src/main/jniLibs/arm64-v8a/libopencv_java4.so    ✅
app/src/main/res/drawable/aim_template.png            ✅
```

全部 ✅ 后，推送到 GitHub，Actions 会自动构建出 APK。
